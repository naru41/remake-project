'use strict'

/**
 * Resourceful controller for interacting with propotybidings
 */
class PropotyBidingController {
  /**
   * Show a list of all propotybidings.
   * GET propotybidings
   */
  async index ({ request, response, view }) {
  }

  /**
   * Render a form to be used for creating a new propotybiding.
   * GET propotybidings/create
   */
  async create ({ request, response, view }) {
  }

  /**
   * Create/save a new propotybiding.
   * POST propotybidings
   */
  async store ({ request, response }) {
  }

  /**
   * Display a single propotybiding.
   * GET propotybidings/:id
   */
  async show ({ params, request, response, view }) {
  }

  /**
   * Render a form to update an existing propotybiding.
   * GET propotybidings/:id/edit
   */
  async edit ({ params, request, response, view }) {
  }

  /**
   * Update propotybiding details.
   * PUT or PATCH propotybidings/:id
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a propotybiding with id.
   * DELETE propotybidings/:id
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = PropotyBidingController
